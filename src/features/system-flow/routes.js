// Base path the whole system-flow module is mounted under (see src/App.jsx).
// Kept in its own file so both the app shell and the landing page can import it
// without a circular dependency.
export const SYSTEM_FLOW_BASE = '/projects/system-flow'
